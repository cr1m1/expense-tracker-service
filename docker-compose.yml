version: '3.8'

services:
  mongodb:
    image: mongo:7-alpine
    container_name: expense_mongodb
    environment:
      MONGO_INITDB_DB: expenses
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data:
